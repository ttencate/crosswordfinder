<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Crossword Finder</title>
    <link rel="icon" href="favicon.ico">
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <h1>Crossword Finder</h1>
    <form>
      <p>Enter the letters you know, using a period (<tt>.</tt>) to indicate unknown letters (blanks). For example: &ldquo;<tt>a..w.r</tt>&rdquo;.</p>
      <fieldset>
        <input id="q" type="text" name="q" value="{{ q }}" autofocus></input>
        <select name="l">
          {% for language in languages %}
            <option value="{{ language }}" {% if language == l %}selected{% endif %}>
              {{ language }}
            </option>
          {% endfor %}
        </select>
        <button type="submit">Search</button>
      </fieldset>
      {% if results %}
        <p class="results">
          Found <strong>{{ results | size }}</strong> results
        </p>
        <ul>
          {% for result in results %}
            <li>{{ result }}</li>
          {% endfor %}
        </ul>
      {% endif %}
    </form>
    <div class="footer">
      <p>A <a href="http://frozenfractal.com/">Frozen Fractal</a> product. Source code on <a href="http://github.com/ttencate/crosswordfinder">GitHub</a>.</p>
    </div>
  </body>
</html>
